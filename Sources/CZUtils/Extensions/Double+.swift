import Foundation

public extension Double {
  /**
   Round the value with 1 decimal. e.g. (5.4873).roundedWithDecimal() = 5.5
   
   TODO: to be updated - use `rounded()` now.
   */
//  func roundedWithDecimal(_ decimal: Int = 1) -> Double {
//    var factor: Double = 1
//    for _ in 0..<decimal {
//      factor *= 10
//    }
//    return (self * factor).rounded() / factor
//  }
}
